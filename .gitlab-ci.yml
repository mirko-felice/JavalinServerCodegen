image: pikalab/ci:ubuntu-jdk14-git-gradle-graphviz

before_script:
  - chmod +x mvnw
  - source $HOME/.sdkman/bin/sdkman-init.sh

Compile:
  stage: build
  script:
    - ./mvnw clean package
    - bin/javalin.sh

Test:
  stage: test
  script:
    - chmod +x gradlew
    - ./gradlew test
